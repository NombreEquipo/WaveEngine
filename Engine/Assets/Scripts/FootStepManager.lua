

function Start(self)
    -- Called once when the script is initialized
    Engine.Log("Script Started!")
    
    -- Example: Get initial position
    local player = self.gameObject.parent
    
end

function Update(self, dt)

end
